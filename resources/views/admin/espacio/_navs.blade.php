{{-- <ul class="nav nav-tabs">
  <li class="nav-item">
    <a class="nav-link {{ activeTab(['admin/espacios/' . $e->id]) }}" aria-current="page" href="{{ route('admin.espacio.show', $e->id) }}">Mi espacio</a>
  </li>
  <li class="nav-item">
    <a class="nav-link {{ activeTab(['admin/espacios/' . $e->id . '/edit']) }}" href="{{ route('admin.espacio.edit', $e->id) }}">Editar</a>
  </li>
  <li class="nav-item">
    <a class="nav-link {{ activeTab(['admin/espacios/' . $e->id . '/compartir']) }}" href="{{ route('admin.espacio.edit', $e->id) }}">Compartir</a>
  </li>
</ul> --}}
